name: Publication guard

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  reject-runtime-data:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Reject private and generated runtime data
        run: python scripts/check_public_tree.py
